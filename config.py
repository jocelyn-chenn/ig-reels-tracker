# config.py
# 這個檔案是整個專案的設定中心
# 所有需要自訂的東西都在這裡

import os
from dotenv import load_dotenv

# 讀取 .env 檔案裡的 API Key
load_dotenv()

# ============================================================
# 你要追蹤的 IG 網紅帳號（填 IG username，不含 @）
# ============================================================
INFLUENCERS = [
    "da_chien_huang",   # 換成真實的 IG 帳號
    "helloiamhook",

]

# ============================================================
# API Keys（從 .env 檔案讀取，不要直接填在這裡）
# ============================================================
GEMINI_API_KEY = os.getenv("GEMINI_API_KEY")

# ============================================================
# 爬蟲設定
# ============================================================
# 每抓一個帳號之間，隨機等待幾秒（避免被 IG 封鎖）
SCRAPER_DELAY_MIN = 3   # 最少等幾秒
SCRAPER_DELAY_MAX = 7   # 最多等幾秒

# GraphQL doc_id（用來抓單篇 Reels 詳細數據）
# 如果這個失效了，照說明文件的步驟更新
IG_DOC_ID = "8845758582119845"

# IG App ID（固定值，通常不會變）
IG_APP_ID = "936619743392459"

# ============================================================
# 業配關鍵字（給 Gemini 參考用，不是拿來直接比對）
# ============================================================
SPONSORED_KEYWORDS = [
    "合作", "業配", "贊助", "sponsored", "collaboration",
    "gifted", "ad", "廣告", "折扣碼", "優惠碼",
    "discount code", "promo code", "感謝品牌",
]

# ============================================================
# 資料庫設定
# ============================================================
DB_PATH = "data/tracker.db"